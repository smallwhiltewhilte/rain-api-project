spring:
  application:
    name: system-service
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: centos7:8848
      config:
        namespace: dev
        group: xuecheng-plus-project
        file-extension: yaml
        refresh-enabled: true
        shared-configs:
          - data-id: logging-${spring.profiles.active}.yaml
            group: xuecheng-plus-common
            refresh: true